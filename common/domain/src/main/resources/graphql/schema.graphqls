schema {
    query: Query
}

type Role {
    code: String!
}

type User {
    oid: ID!,
    userId: String!,
    name: String!,
    email: String!,
    role: String!,
    status: String!
}

type Query {
    findAllUsers: [User],
    findByUserId(userId: String!): User
}
